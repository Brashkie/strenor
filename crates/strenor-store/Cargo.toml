[package]
name = "strenor-store"
description = "Pure key-value core for Strenor (in-memory byte store, TTL, AOF, snapshot)"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
parking_lot = "0.12"

[dev-dependencies]
criterion = { version = "0.5", default-features = false }

[[bench]]
name = "engine"
harness = false
